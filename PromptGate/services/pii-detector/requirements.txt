# Presidio PII Detection Service Dependencies
fastapi==0.115.1
uvicorn==0.34.0
pydantic==2.6.4
httpx==0.28.1
loguru==0.7.2

# Presidio PII Detection (호환성 문제로 제거)
# presidio-analyzer==2.2.360
# presidio-anonymizer==2.2.360

# 한국어 처리 (가벼운 버전)
konlpy>=0.6.0

# 검증기 라이브러리
phonenumbers>=8.12.0
python-stdnum>=1.17
email-validator>=2.0

# 기타 유틸리티
toml>=0.10.2
python-dotenv==1.1.0
